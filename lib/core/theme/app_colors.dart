import 'package:flutter/material.dart';

class AppColors {
  // Transparent Color
  static const Color transparentColor = Colors.transparent;

  // Primary Colors
  static const Color primary = Color(0xFFA68CF3);
  static const Color primaryLight = Color(0xFFECE7FD);

  // Secondary Color
  static const Color secondary = Color(0xFF03DAC5);

  // Background / Base Colors
  static const Color background = Color(0xFFFFFFFF);

  // Text Colors
  static const Color textPrimary = Color(0xFF020817);
  static const Color textSecondary = Color(0xFF64758B);

  // Button & Border Colors
  static const Color btnBorderColor = Color(0xFFE5E7EB);
  static const Color textFieldBorderColor = Color(0xFFE2E8F0);
  static const Color textFieldHintColor = Color(0xFF64758B);

  // Text Button Color
  static const Color textBtnColor = Color(0xFFA68CF3);

  // TextFiled Error Color
  static const Color textFieldErrorBorderColor = Color(0xFFF0453A);

  // Snackbar Colors
  static const Color snackBarSuccessBgColor = Color(0xFFE5F3E5);
  static const Color snackBarSuccessTextColor = Color(0xFF44A246);
  static const Color snackBarFailureBgColor = Color(0xFFFDE4E1);
  static const Color snackBarFailureTextColor = Color(0xFFF0453A);
  static const Color snackBarCloseIconColor = Color.fromARGB(
    255,
    106,
    106,
    106,
  );

  // Jar Colors
  static const Color necessitiesColor = Colors.blueAccent;
  static const Color financialFreedomColor = Colors.green;
  static const Color educationColor = Colors.orange;
  static const Color savingsColor = Colors.purple;
  static const Color playColor = Colors.redAccent;
  static const Color giveColor = Colors.teal;

  // Profile list tile
  static const Color profileListTileColor = Color(0xFF9EA0A2);
}
